# Phase 1 runtime deps.
# stable-baselines3 arrives with the PPO loop (M5).
poke-env>=0.15.0
numpy>=1.24
torch>=2.0
# Note: the CPU-only torch wheels live on download.pytorch.org, which may be
# blocked by a restrictive network policy. The default PyPI torch wheel works on
# CPU too (its bundled CUDA libs simply go unused). See README.
